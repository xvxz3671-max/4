# Полное руководство по деплою Maratik Coach на Vercel

## Подготовка к деплою

### 1. Создание Telegram бота
1. Напишите @BotFather в Telegram
2. Выполните команду `/newbot`
3. Следуйте инструкциям и получите BOT_TOKEN
4. Сохраните токен - он понадобится позже

### 2. Подготовка репозитория
1. Загрузите код в GitHub репозиторий
2. Убедитесь, что `.env` файл НЕ попал в репозиторий (он в .gitignore)

## Деплой на Vercel

### Шаг 1: Создание проекта для Web App

1. Зайдите на https://vercel.com и войдите в аккаунт
2. Нажмите "New Project"
3. Выберите ваш GitHub репозиторий `maratik-coach`
4. Настройте проект:
   - **Project Name**: `maratik-coach-web`
   - **Framework Preset**: Vite
   - **Root Directory**: `apps/web`
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
   - **Install Command**: `npm install`

5. В разделе "Environment Variables" добавьте:
   ```
   VITE_API_BASE_URL = https://maratik-coach-api.vercel.app
   ```
   (URL будет известен после создания API проекта)

6. Нажмите "Deploy"

### Шаг 2: Создание проекта для API

1. Снова нажмите "New Project" на Vercel
2. Выберите тот же GitHub репозиторий
3. Настройте проект:
   - **Project Name**: `maratik-coach-api`
   - **Framework Preset**: Other
   - **Root Directory**: `apps/api`
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
   - **Install Command**: `npm install`

4. В разделе "Environment Variables" добавьте:
   ```
   DATABASE_URL = postgresql://username:password@host:port/database
   ```
   
   **Варианты для базы данных:**
   
   **Вариант A: Vercel Postgres (рекомендуется)**
   - В проекте API перейдите в Storage → Create Database → Postgres
   - Vercel автоматически добавит DATABASE_URL
   
   **Вариант B: Supabase (бесплатно)**
   - Создайте проект на https://supabase.com
   - Скопируйте Connection String из Settings → Database
   - Формат: `postgresql://postgres:[password]@[host]:5432/postgres`
   
   **Вариант C: Railway**
   - Создайте PostgreSQL на https://railway.app
   - Скопируйте DATABASE_URL из переменных

5. Нажмите "Deploy"

### Шаг 3: Обновление URL в Web App

1. После деплоя API скопируйте его URL (например: `https://maratik-coach-api.vercel.app`)
2. Вернитесь к проекту Web App
3. Перейдите в Settings → Environment Variables
4. Обновите `VITE_API_BASE_URL` на актуальный URL API
5. Перейдите в Deployments и нажмите "Redeploy" на последнем деплое

### Шаг 4: Связывание проектов (Related Projects)

1. В проекте Web App перейдите в Settings → General
2. Найдите раздел "Related Projects"
3. Добавьте API проект (`maratik-coach-api`)
4. Это позволит preview-деплоям автоматически использовать preview API

## Деплой бота

Бот не может быть задеплоен на Vercel. Используйте одну из альтернатив:

### Вариант A: Railway (рекомендуется)

1. Зайдите на https://railway.app
2. Создайте новый проект "Deploy from GitHub repo"
3. Выберите ваш репозиторий
4. Настройте:
   - **Root Directory**: `apps/bot`
   - **Build Command**: `npm run build`
   - **Start Command**: `npm start`

5. Добавьте переменные окружения:
   ```
   BOT_TOKEN = ваш_токен_от_botfather
   API_BASE_URL = https://maratik-coach-api.vercel.app
   PUBLIC_WEBAPP_URL = https://maratik-coach-web.vercel.app
   NODE_ENV = production
   ```

6. Нажмите Deploy

### Вариант B: Fly.io

1. Установите Fly CLI: https://fly.io/docs/getting-started/installing-flyctl/
2. В директории `apps/bot` выполните:
   ```bash
   flyctl auth login
   flyctl launch
   ```
3. Настройте переменные:
   ```bash
   flyctl secrets set BOT_TOKEN=ваш_токен
   flyctl secrets set API_BASE_URL=https://maratik-coach-api.vercel.app
   flyctl secrets set PUBLIC_WEBAPP_URL=https://maratik-coach-web.vercel.app
   ```
4. Деплой:
   ```bash
   flyctl deploy
   ```

## Настройка Telegram бота

### 1. Настройка Menu Button
1. Напишите @BotFather
2. Выполните команду `/setmenubutton`
3. Выберите вашего бота
4. Введите:
   - **Text**: `🏋️ Открыть приложение`
   - **URL**: `https://maratik-coach-web.vercel.app`

### 2. Настройка описания бота
```
/setdescription
@ваш_бот
Maratik Coach - персональный тренер в Telegram! 💪

🏋️ Планирование тренировок
📊 Отслеживание прогресса  
🔥 Мотивация и стрики
📱 Удобное Mini App

Начни тренироваться уже сегодня!
```

### 3. Настройка команд
```
/setcommands
@ваш_бот

start - Начать работу с ботом
plan - Генерация плана тренировок
help - Справка по командам
```

## Проверка работы

### 1. Проверьте Web App
- Откройте `https://maratik-coach-web.vercel.app`
- Должна загрузиться страница с Маратиком
- Проверьте, что API отвечает (статистика загружается)

### 2. Проверьте API
- Откройте `https://maratik-coach-api.vercel.app/api/health`
- Должен вернуться JSON с `{"status": "ok"}`

### 3. Проверьте бота
- Напишите боту `/start`
- Должно появиться приветствие с кнопкой Mini App
- Нажмите кнопку - должно открыться приложение

### 4. Проверьте интеграцию
- В Mini App завершите тренировку
- Бот должен прислать сообщение о завершении

## Мониторинг и логи

### Vercel
- Логи доступны в разделе "Functions" каждого проекта
- Мониторинг в разделе "Analytics"

### Railway
- Логи в разделе "Deployments" → "View Logs"
- Метрики в разделе "Metrics"

### Fly.io
```bash
flyctl logs
flyctl status
```

## Troubleshooting

### Проблема: API не отвечает
- Проверьте логи в Vercel Functions
- Убедитесь, что DATABASE_URL корректный
- Проверьте, что Prisma схема применена

### Проблема: Mini App не загружается
- Проверьте CORS настройки в API
- Убедитесь, что VITE_API_BASE_URL правильный
- Проверьте HTTPS (обязательно для Telegram)

### Проблема: Бот не отвечает
- Проверьте правильность BOT_TOKEN
- Убедитесь, что webhook не установлен
- Проверьте логи деплоя бота

### Проблема: База данных
- Для Vercel Postgres проверьте подключение в Storage
- Для внешних БД проверьте доступность хоста
- Убедитесь, что схема Prisma применена

## Готово! 🎉

Ваш Maratik Coach готов к использованию:
- ✅ Web App на Vercel
- ✅ API на Vercel  
- ✅ Bot на Railway/Fly.io
- ✅ База данных настроена
- ✅ Telegram интеграция работает

Поделитесь ботом с друзьями и начинайте тренироваться! 💪